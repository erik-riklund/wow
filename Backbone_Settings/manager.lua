---@meta

--[[~ Updated: 2024/11/29 | Author(s): Gopher ]]

-- ---@class SettingsManager
-- local manager = context.manager

---param options SettingsElement.Header
---?
-- manager.createHeader = function (self, options)
--   self.layout:AddInitializer(
--     CreateSettingsListSectionHeaderInitializer(options.text, options.tooltip)
--   )
-- end

-- ---@param options SettingsElement.Checkbox
-- ---?
-- manager.createCheckbox = function (self, options)
--   -- local canLogSetting = Settings.RegisterProxySetting(category, "MY_ADDON_CAN_LOG", Settings.VarType.Boolean,
--   --  'what!', Settings.Default.True, function () return true end, function (value) end)

--   -- local setting = Settings.RegisterProxySetting(

--   -- )

--   --Settings.CreateCheckbox(self.category, canLogSetting, "And so the white dog ate the black cat and turned into a brown frog. The moral of the story is absent but rather implicit. Do not eat cats and dogs. Listen to Trump.")
-- end
