
-- local category, layout = Settings.RegisterVerticalLayoutCategory 'Test'
-- local subcategory = Settings.RegisterVerticalLayoutSubcategory (category, 'Subtest')

-- Settings.RegisterAddOnCategory (category)

-- for key, value in pairs(Settings) do
--   print (string.format ('[%s] = %s', key, type (value)))
-- end

--- name string tooltip string
-- layout:AddInitializer(CreateSettingsListSectionHeaderInitializer('Hello world', 'dlrow olleH'));

-- local canLogSetting = Settings.RegisterProxySetting(category, "MY_ADDON_CAN_LOG", Settings.VarType.Boolean,
--   'what!', Settings.Default.True, function () return true end, function (value) end)
-- Settings.CreateCheckbox(category, canLogSetting, "And so the white dog ate the black cat and turned into a brown frog. The moral of the story is absent but rather implicit. Do not eat cats and dogs. Listen to Trump.")
